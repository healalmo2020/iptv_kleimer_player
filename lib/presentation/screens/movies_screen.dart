import 'section_screen.dart';

class MoviesScreen extends SectionScreen {
  const MoviesScreen({super.key})
      : super(
          title: 'Movies',
          description: 'Módulo VOD preparado para Fase 2 (categorías, listado y player).',
        );
}
