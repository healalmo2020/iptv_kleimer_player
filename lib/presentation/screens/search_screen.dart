import 'section_screen.dart';

class SearchScreen extends SectionScreen {
  const SearchScreen({super.key})
      : super(
          title: 'Search',
          description: 'Buscador global preparado para integrar Live, VOD y Series.',
        );
}
