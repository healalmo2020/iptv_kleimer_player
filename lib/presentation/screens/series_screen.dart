import 'section_screen.dart';

class SeriesScreen extends SectionScreen {
  const SeriesScreen({super.key})
      : super(
          title: 'Series',
          description: 'Módulo Series preparado para Fase 2 (Series → Seasons → Episodes).',
        );
}
