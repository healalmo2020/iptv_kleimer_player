import 'section_screen.dart';

class HistoryScreen extends SectionScreen {
  const HistoryScreen({super.key})
      : super(
          title: 'History',
          description: 'Historial de reproducción persistente y ordenado por fecha.',
        );
}
